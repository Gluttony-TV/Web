type Query {
   getShow(id: Int!): Show!
   searchShows(by: String!): [Show!]
}

type Show {
   id: Int!
   name: String!
   slug: String!
   image: String!
   thumbnail: String
   first_air_time: String
   next_air_time: String
   year: String
   score: String
   status: Status!
   overview: String!
   originalNetwork: Network!
   episodes: [Episode!]!
   seasons: [Season!]!

   characters: [Character!]!
   genres: [Genre!]!
   networks: [Network!]!

   isIn(slug: String, primary: Boolean): Boolean
   progress: Progress
}

type Status {
   id: Int!
   name: String!
   recordType: String!
   keepUpdated: Boolean!
}

type Character {
   id: Int!
   name: String!
   image: String!
}

type Genre {
   id: Int!
   name: String!
   slug: String!
}

type Artwork {
   id: Int!
   image: String!
   thumbnail: String!
   type: Int!
   score: [Int!]!
}

type Network {
   id: Int!
   name: String!
   slug: String!
   abbreviation: String!
   country: String!
}
