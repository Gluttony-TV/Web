type Query {
   getOwnLists: [List!]!
   getListsOf(user: ID!): [List!]!

   getOwnList(slug: String!): List!
   getListOf(slug: String!, user: ID!): List!
   getList(id: ID!): List!

   isInList(show: Int!, slug: String, primary: Boolean): Boolean!
}

type Mutation {
   addToList(shows: [Int!]!, slug: String, primary: Boolean): List!
   removeFromList(shows: [Int!]!, slug: String, primary: Boolean): List!
}

type List {
   id: ID!
   name: String!
   slug: String!
   public: Boolean!
   primary: Boolean!
   userId: ID!
   shows: [ListEntry!]
}

type ListEntry {
   id: Int!
   addedAt: String!
   show: Show!
}
